<%= form_with(model: actor, local: true) do |form| %>
  <% if actor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(actor.errors.count, "error") %> prohibited this actor from being saved:</h2>

      <ul>
      <% actor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row center">
    <div class="field col s4 offset-s4 ">
      <%= form.label :Nombre %>
      <%= form.text_field :name, id: :actor_name %>
      <%= form.label :Fehca_de_nacimiento %>
      <%= form.date_field :birth_date,start_year: 1990, id: :actor_birth_date, class: "datepicker" %>
      <div class="actions">
        <%= form.submit %>
      </div>
    </div>
  </div>
<% end %>
